#include<stdio.h>
#include<math.h>

int main(int argc, char const *argv[])
{
    int num;
    float raiz;

    printf("Digite um numero inteiro: ");
    scanf("%d", &num);
    if(num % 2 == 0){
        raiz = sqrt("A raiz quadrada e: %.2f", raiz);
        }
    return 0;
}
